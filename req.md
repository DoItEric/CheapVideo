# html-ppt

## 需求说明
本项目是一个ppt模板，旨在作为skill为AI提供一套标准的HTML ppt生成框架。
本模板采用纯HTML本地运行纯静态的方式呈现，不适用VUE甚至npm等复杂的包管理器。（可加载在线资源，比如js或css之类的）
本模板集成了echats、threeJS等多种炫酷的动效库，支持各种翻页、动态数据呈现的展示效果。
本模板提供多种内容结构形式，就像PowerPoint一样，可以有多种布局。比如：纯标题、标题文本、标题图表、标题文本图表、动画效果、纯自定义等等。
本模板提供多种切换效果，包括不限于滑动、翻页、扯画布。
本模板提供一个README.md说明文件，用来告诉用户和AI本项目的结构和使用方法。
本模板支持多种样式定义，可使用tailwind或者其他成熟的样式框架进行实现，前提是不能用npm这种繁重的库，一定要纯静态实现。

## 项目目录结构

html-ppt
  |-  index.html #ppt主页面，负责切换动画、自动播放控制、上一页下一页等操作
  |-  pages                                         # 负责放各种最终页面 
        |-  page1.html                              # 实际页面
        |-  page2.html 
        |-  ...
  |-  templates                                     # 各种模板存放的位置
        |-  title.html #纯标题页面
        |-  title_content.html                      # 标题+文字内容
        |-  ....
        |-  custom.html #纯定制页面
  |-  styles                                        # 统一样式目录
        |-  ppt.css                                 # 主框架+页面样式（固定引用）

## 具体技术要求
### 1. index.html的职责
- index.html是整个项目的框架页，提供4:3比例的中央展示区域；
- 维护着一个页面列表，建立顺序和序号、持续时间；
- 提供上一页、下一页、自动播放/暂停播放的操作按钮；这几个操作按钮可通过整个页面右下角的悬浮按钮点击隐藏；
- 在中央展示区下方展示一个播放进度条，按时间/页面顺序展示当前进度；

### 2.templates文件夹的作用
templates文件主要是为用户和AI提供一个参考，告诉他们应该按照什么样子的格式来写；
每一个page都分为三个区域：页眉、内容、页脚，其中页眉用来放当前页的小标题，居左；内容可自动生成、页脚在局右位置显示一个小小的页码；

### 3.布局和样式
本项目生成的html ppt 将用作互联网项目的介绍视频，所以要炫酷、吸引人并充满科技感

### 4. 固定样式文件
取消 themes 机制和 URL 参数样式绑定。项目只保留一个固定样式文件 `styles/ppt.css`：
- `index.html` 固定引用 `./styles/ppt.css`；
- `pages/*.html` 和 `templates/*.html` 固定引用 `../styles/ppt.css`；
- 用户和 AI 只需要维护这一个样式文件。

### 5. 关于readme.md
README 必须包含“如何新增页面、如何修改样式、如何配置播放顺序”这三段。

## 实际的应用场景
在实际使用时，用户会将README.md 与本项目所有代码一起丢给AI，然后AI将读取README与template文件夹中的内容，并根据规则生成代码。

